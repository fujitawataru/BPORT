<!DOCTYPE html>
<html>
  <head>
    <title>BPORT</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src="https://kit.fontawesome.com/2370f14d43.js" crossorigin="anonymous"></script>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
     <header>
       <nav class="navbar navbar-expand-lg navbar-dark bg-secondary">
        <div class="container">
          <a class="navbar-brand" href="/">BPORT</a>
          <% if current_user %>
             <div>ようこそ、<%= current_user.name %>さん！</div>
          <% end %>

          <ul class= "navbar-nav ml-auto">

            <% if user_signed_in? %>

             <li class="nav-item">
             <%= link_to "ログアウト", destroy_user_session_path, method: :delete,class:"nav-link text-light" %>
             </li>

             <li class="nav-item">
                 <%= link_to "マイページ", user_path(current_user.id),class:"nav-link text-light" %>
             </li>

             <li class="nav-item">
                 <%= link_to "投稿一覧", posts_path,class:"nav-link text-light" %>
             </li>

              <li class="nav-item">
                 <%= link_to "お問い合わせ", new_inquiry_path,class:"nav-link text-light" %>
             </li>

             <li class="nav-item">
                 <%= link_to "メッセージ", rooms_path,class:"nav-link text-light" %>
             </li>

             <li class="nav-item">
                 <%= link_to "通知", notifications_path,class:"nav-link text-light" %>
             </li>



            <% else %>

             <li class="nav-item">
              <%= link_to "新規登録",new_user_registration_path,class:"nav-link text-light" %>
             </li>

             <li class="nav-item">
              <%= link_to"ログイン",new_user_session_path,class:"nav-link text-light" %>
             </li>

             <li class="nav-item">
                 <%= link_to "お問い合わせ", new_inquiry_path,class:"nav-link text-light" %>
             </li>

            <% end %>
          </ul>
        </div>
       </nav>
     </header>
     <main>

      <% flash.each do |key, value| %>
      <%= content_tag(:div, value, class: "#{key}") %>
    <% end %>

      <%= yield %>
     </main>
  </body>
</html>
