<div class="container">
  <div class="row">
    <div class="col-lg-12">
      <h4>通知</h4>
      <%= link_to "通知削除", notification_path(@notifications), method: :delete %>
      <% if @notifications.exists? %>
       <%= render @notifications %>
      <% else %>
      <p>通知はありません</p>
      <% end %>
    </div>
  </div>
</div>